import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function piOpenAiLimits(_pi: ExtensionAPI): void {
  // Extension behavior is specified through the Trellis project interview.
}
